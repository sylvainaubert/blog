{% extends 'base.html.twig' %}

{% block title %}Articles par tag{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
    </style>

    <div class="example-wrapper">

        <h1>Liste des Articles par : {{ tags.name }}</h1>

        {% for article in tags.articles %}
            <li>{{ article.id }} {{ article.title }} </li>
        {% else %}
            <li>No articles</li>
        {% endfor %}

    </div>
{% endblock %}
