{% extends 'base.html.twig' %}

{% block title %}Articles par tag{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
    </style>

    <div class="example-wrapper">

        <h1>Liste des Articles par : {{ tag.name }}</h1>

        {% for article in tag.articles %}
            <li><a href="{{ path('article_show', {'id': article.id}) }}">Id n° {{ article.id }} / {{ article.title }}</a></li>
        {% else %}
            <li>No articles</li>
        {% endfor %}

    </div>
{% endblock %}
